apiVersion: v2
name: ferment
description: A starter chart for helmetica transmuter charts

# Transmuter charts are always application charts
type: application

version: 0.0.26

appVersion: "1.16.0"

dependencies:
  # The framework's shared templates. templates/azoth.yaml is what renders them.
  - name: azoth
    version: 0.0.2
    repository: oci://ghcr.io/helmetica-framework
